# Training deps for the edge device. Install into a venv with --system-site-packages
# if you want to reuse JetPack's CUDA stack.
#
# PyTorch first — on JetPack 6 (CUDA 12.6, SBSA-compatible) the standard
# aarch64 CUDA wheels work:
# pip install torch --index-url https://download.pytorch.org/whl/cu126
# If that ever breaks, fall back to NVIDIA's edge device wheels or edge device-containers.

transformers>=4.44
trl>=0.10
peft>=0.12
datasets>=2.20
accelerate>=0.33
bitsandbytes>=0.43 # see README: aarch64 builds can be the fiddly part
sentencepiece
